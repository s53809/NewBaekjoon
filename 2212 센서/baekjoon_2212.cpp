#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(nullptr);

	int N, K;
	int x[10001];
	cin >> N >> K;
	for (int i = 0; i < N; i++) {
		cin >> x[i];
	}
}