#include <iostream>
#include <string.h>
#include <vector>
using namespace std;

int N, M;
vector<string> map;
vector<vector<int>> distance;

void DFS(int str_x,int str_y) {

}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> N >> M;
	map.resize(N);
	for (int i = 0; i < N; i++) {
		cin >> map[i];
	}
	
}